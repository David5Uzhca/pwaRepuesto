export class Calificacion{
    uid?: string;
    materia?: string; //Aqui con el signo de pregunta, no es necesario definir la variable con algun valor y quedaria como valor undefined
    nota?: string;
    fecha?: string;
}